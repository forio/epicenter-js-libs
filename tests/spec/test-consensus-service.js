var ConsensusService = F.service.Data;

var account = 'forio';
var project = 'js-libs';

var baseURL = (new F.service.URL({ accountPath: account, projectPath: project })).getAPIPath('data');

describe('Consensus Service', function () {
    describe('delete', function () {
        it('Should do a DELETE', function () {

        });
    });
});